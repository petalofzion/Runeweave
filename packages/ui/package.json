{
  "name": "@runeweave/ui",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "svelte": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "echo \"(todo)\" && exit 0",
    "fmt": "echo \"(prettier via pre-commit)\" && exit 0"
  },
  "devDependencies": {
    "svelte": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
