{
  "name": "@runeweave/adapters",
  "version": "0.0.1",
  "type": "module",
  "main": "src/model.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "echo \"(todo)\" && exit 0",
    "fmt": "echo \"(prettier via pre-commit)\" && exit 0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
